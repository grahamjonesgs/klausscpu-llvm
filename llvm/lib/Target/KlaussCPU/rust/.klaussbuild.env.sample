# Copy to .klaussbuild.env (gitignored) and edit. Sourced by ./klauss-build.
export KLAUSSCPU_RUSTC="$HOME/Documents/src/klausscpu-rust/build/x86_64-apple-darwin/stage1/bin/rustc"
# Optional overrides (sensible defaults otherwise):
# export KLAUSSCPU_LLVM_BIN=<repo>/build/bin          # default: repo-relative
# export KLAUSSCPU_LD=<path>/klausscpu.ld             # default: bundled rust/klausscpu.ld
# export KLAUSSCPU_RUNTIME="$HOME/Documents/src/klausscpu-runtime"  # enables import cross-check
